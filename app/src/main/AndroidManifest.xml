<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mursyidul.task1mvp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/photo_2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/photo_2"
        android:supportsRtl="true"
        android:theme="@style/Theme.Task1mvp">
        <activity android:name=".view.FaktorialActivity"></activity>
        <activity android:name=".view.DetikActivity" />
        <activity android:name=".view.PangkatActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>